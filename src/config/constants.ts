export const CONSTANTS = {
    // Hand Tracking Configuration
    GESTURE_CONFIDENCE_THRESHOLD: 0.8,

    // Game Timing Configuration (milliseconds)
    COUNTDOWN_DURATION: 1000,     // 1 second per count (3, 2, 1)
    SHOOT_DELAY: 500,             // Wait 500ms on "Shoot" before showing results
    RESULT_DURATION: 3000,        // Show results for 3 seconds before resetting

    // UI Configuration
    COLORS: {
        NEON_GREEN: '#39ff14',
        NEON_PINK: '#ff00ff',
        NEON_BLUE: '#0ff0fc',
        BACKGROUND: '#0a0a0a'
    }
};
